@page "/services"
@inject HttpClient Http

<h1>Services</h1>

@if (_items == null)
{
    <p><em>Loading...</em></p>
}
else
{
    @foreach (var item in _items)
    {
        <p><ServiceItem Data="@item"></ServiceItem></p>
    }
}


@code {
    private Savitar.Web.Client.Models.Service[] _items;

    protected override async Task OnInitializedAsync()
    {
        _items = await Http.GetFromJsonAsync<Savitar.Web.Client.Models.Service[]>("api/data/GetServices");
    }
}